spring:
  application:
    name: lfg-platform

  datasource:
    url: jdbc:postgresql://localhost:5432/dev-lfg-db
    username: nanaut
    password: nanaut
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true

logging:
  level:
    org.flywaydb: INFO